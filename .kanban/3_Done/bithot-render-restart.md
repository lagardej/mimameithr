---
status: done
area: Skald/Bithot
---

# Bithot render restart

`BootTest.cs` restarted. Lit sphere, orbital camera,
`EquatorGuide`/`RotationAxisGuide` as separate opt-in building blocks.
`EngineBootstrap.cs` boots headless `Nornir`, seeds one body, runs
`SetTectonicsMobileLid`.
