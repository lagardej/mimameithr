= Backlog
:toc: left

Actionable tasks and ideas, tracked by status. `concerns.adoc` describes current architecture and drifts out of date if tasks aren't logged here as they land — update both when a task changes what a concern owns/reads/exposes.

Status tags: `TODO`, `IN PROGRESS`, `DEFERRED` (good idea, no consumer/prerequisite yet), `REJECTED` (considered, not doing — reason kept for reference).

== Bifrost / Skald (Tooling & Rendering)

_Nothing outstanding._

== Hlothyn — Tectonics

* `TODO` — Per-plate-pair boundary classification. Currently per-cell (a cell's boundary type doesn't know which two plates it's between). Prerequisite for Orogeny belt coherence.
* `TODO` — `ILinkRelation` (Friflo) for Orogeny belt-to-cell relationships. Blocked on per-plate-pair classification above.
* `DEFERRED` — Extension rate (horizontal stretching) per plate boundary. Physically sound, but `Logr` (basin/river) and `Geology`/`Sedimentation` are still stub-only in `concerns.adoc` — no consumer yet. Revisit when Logr basin geometry or Sedimentation depocenters are designed. Would feed graben basin capacity, endorheic sink detection, crustal-thinning heat anomalies, sediment accumulation rate.
* `REJECTED` — Discrete fault lines (vector line traces). Wrong shape for the per-cell scalar model — needs a geometry/graph layer, new component type, render concern. Disproportionate for a civilization-scale sim. `Dynamics` already uses designer-scaled indices (1–10) for volcanic/seismic activity; boundary-proximity noise is a cheaper proxy for linear-looking volcanism/drainage if ever needed.

== Kjarni — Geimr

* `TODO` — Orbit system rewrite: derive from position/velocity state vectors instead of Keplerian elements. Position is now the authoritative source (`Kjarni/Nornir/Geimr/Position/`).

== Cleanup

_Nothing outstanding._

== Known limitations (not yet actionable)

* Orogeny belt grouping is spatial-only (flood fill over convergent cells) — merges cells from unrelated plate pairs into one belt. Fixed by the per-plate-pair classification item above.
